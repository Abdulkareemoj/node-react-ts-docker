version: "3.8"
services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      NODE_ENV: production
      PORT: 80
      CORS_ORIGIN: http://localhost
      # Add other necessary environment variables for your backend
      MONGO_URI: your_mongo_uri
      JWT_SECRET: your_jwt_secret